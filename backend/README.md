## Code challenge

Install

Make .env file, create db add your credentials too
```
cp .env.example .env
php artisan key:generate
php artisan migrate
```

After cloning run this
```
composer install
npm install
npm run dev
```